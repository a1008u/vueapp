version: 2
jobs:
  build:
    docker:
      - image: ccircleci/node:11.12.0
    steps:
      - run: echo "hello world"
      - checkout
      - run:
          name: Install dependencies
          command: yarn install
          working_directory: /home/circleci/project/lunch-firebase/
      - run:
          name: unit test exec
          command: yarn run test:unit
          working_directory: /home/circleci/project/lunch-firebase/